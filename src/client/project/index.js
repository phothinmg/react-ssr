import React from "react";

/**
 * A React functional component that renders a heading element
 * containing the output of the Test component.
 */
export const Project = () => {
  return <h1>Projects Page</h1>;
};
